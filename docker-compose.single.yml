version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.single
    ports:
      - "${PORT:-2233}:5000"
    environment:
      - ASPNETCORE_URLS=http://+:5000
      - ASPNETCORE_ENVIRONMENT=Production
      - ConnectionStrings__DefaultConnection=Data Source=/app/data/carbudget.db
    volumes:
      - ./data:/app/data
    restart: unless-stopped
